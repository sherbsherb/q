import styled from 'styled-components'

export const SpanStyled = styled.span`
  margin-left: 10px;
  width: calc(100% - 70px);
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
`
